<h1> To Do List </h1>
<%= link_to 'Add Item', new_todo_path %>
<% @todo.each do |listed| %>
	<% if listed.complete %>
		<h3><strike><%= link_to listed.item, todo_path(listed) %></strike></h3>
	<% else %>
		<h3><%= link_to listed.item, todo_path(listed) %></h3>
	<% end %>
<% end %>	
