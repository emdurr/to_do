<h1>Add a new To Do Item !!</h1>
<%= form_for @todo do |f| %>
	<%= f.label :item %>
	<%= f.text_field :item %>
	<%= f.label :description %>
	<%= f.text_field :description %>
	<%= f.submit %>
<% end %>